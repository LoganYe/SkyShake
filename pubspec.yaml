name: skyshake
description: "SkyShake is a Flutter app for flight turbulence intelligence."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  flutter_map: ^7.0.2
  google_fonts: ^6.2.1
  intl: ^0.20.2
  latlong2: ^0.9.1
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
